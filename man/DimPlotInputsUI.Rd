% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dimPlot_module_ui.R
\name{DimPlotInputsUI}
\alias{DimPlotInputsUI}
\title{Input UI components for the DimPlot module}
\usage{
DimPlotInputsUI(id, data, defaults = NULL, title = NULL, columns = 2)
}
\arguments{
\item{id}{The ID for the Shiny module.}

\item{data}{The data frame used for plot generation.}

\item{defaults}{A named list of default values for the inputs.}

\item{title}{An optional title for the UI grid.}

\item{columns}{Number of columns for the UI grid.}
}
\value{
A Shiny tagList containing the UI elements
}
\description{
This should be placed in the UI where the inputs should be shown, with an \code{id}
that matches the \code{id} used in the \code{DimPlotServer()} and \code{DimPlotOutputUI()} functions.
}
\details{
The user inputs for this module are separated from the outputs to allow for
more flexible UI design.

The inputs will automatically be organized into a grid layout via the \code{organize_inputs()} function,
with \code{columns} controlling the number of columns in the grid.

Defaults can be set for each input by providing a named list of values to the \code{defaults} argument.
Nearly all parameters for \code{\link[plotthis:DimPlot]{plotthis::DimPlot()}} can be set via these inputs, so see the help
for that function for an exhaustive list.
}
\examples{
library(vizModules)
# Create example data with dimension reduction coordinates
set.seed(123)
df <- data.frame(
    dim1 = rnorm(100),
    dim2 = rnorm(100),
    cluster = sample(c("A", "B", "C"), 100, replace = TRUE),
    celltype = sample(c("Type1", "Type2"), 100, replace = TRUE)
)
DimPlotInputsUI("dimPlot", df)
}
\seealso{
\code{\link[plotthis:DimPlot]{plotthis::DimPlot()}}, \code{\link[=organize_inputs]{organize_inputs()}},
\code{\link[=DimPlotOutputUI]{DimPlotOutputUI()}}, \code{\link[=DimPlotServer]{DimPlotServer()}}, \code{\link[=DimPlotApp]{DimPlotApp()}}
}
\author{
Jared Andrews
}
