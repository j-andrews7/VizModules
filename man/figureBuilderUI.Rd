% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/figureBuilder_module_ui.R
\name{figureBuilderUI}
\alias{figureBuilderUI}
\title{UI component for the Figure Builder module}
\usage{
figureBuilderUI(id, title = "VizModules Figure Builder")
}
\arguments{
\item{id}{The ID for the Shiny module. Must match the \code{id} given to
\code{\link[=figureBuilderServer]{figureBuilderServer()}}.}

\item{title}{A character string used as the header shown above the builder
(default: \code{"VizModules Figure Builder"}). Pass \code{NULL} to omit the header,
which is useful when the host page supplies its own title.}
}
\value{
A Shiny \code{tagList} containing the Figure Builder UI.
}
\description{
Renders the full multi-panel \strong{Figure Builder} interface (sidebar controls
plus the free-form A4 canvas) as a namespaced Shiny module. Place this in the
UI where you want the builder to appear, using an \code{id} that matches the one
passed to \code{\link[=figureBuilderServer]{figureBuilderServer()}}.
}
\details{
Unlike \code{\link[=figureBuilderApp]{figureBuilderApp()}} (which returns a complete, standalone app), this
function returns a \code{tagList} you can drop into any page, so the builder can be
embedded alongside other content and instantiated more than once (each
instance keeps its own namespace, canvas, and downloads).

The returned UI bundles the JavaScript and CSS the canvas needs, and calls
\code{\link[shinyjs:useShinyjs]{shinyjs::useShinyjs()}}, so no extra setup is required in the host app.
}
\examples{
library(VizModules)
figureBuilderUI("figure_builder")
}
\seealso{
\code{\link[=figureBuilderServer]{figureBuilderServer()}}, \code{\link[=figureBuilderApp]{figureBuilderApp()}}
}
\author{
Jared Andrews
}
