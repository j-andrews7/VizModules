% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_backends.R
\name{register_model_backend}
\alias{register_model_backend}
\title{Register a model backend}
\usage{
register_model_backend(name, backend)
}
\arguments{
\item{name}{Character string. The name that will appear in the model-type
dropdown (e.g. \code{"drm"}, \code{"gam"}).}

\item{backend}{A named list with elements \code{fit}, \code{predict}, and
\code{validate_classes} as described above.}
}
\value{
Invisibly returns \code{NULL}. Called for its side effect.
}
\description{
Add or replace a model backend in the registry. Once registered, the backend
name appears in the model-type dropdown and the custom-model-lines pipeline
dispatches through it automatically.
}
\details{
A backend is a named list with three required elements:
\describe{
\item{\code{fit}}{A function with signature \verb{function(formula, data, ...)} that
returns a fitted model object. Extra UI fields from the
\code{\link[=multiDynamicInput]{multiDynamicInput()}} row are forwarded as \code{...}.}
\item{\code{predict}}{A function with signature \verb{function(model, newdata)} that
returns a numeric vector of predicted y-values, one per row of
\code{newdata}.}
\item{\code{validate_classes}}{Character vector of class names. After fitting,
the pipeline checks \code{inherits(model, validate_classes)} and rejects the
model if it fails.}
}
}
\examples{
# Register a custom backend for dose-response curves (requires drc)
if (requireNamespace("drc", quietly = TRUE)) {
    register_model_backend("drm", list(
        fit = function(formula, data, drc_fct = "LL.4", ...) {
            fct_map <- list(
                "LL.4" = drc::LL.4, "LL.3" = drc::LL.3,
                "LL.2" = drc::LL.2, "W1.4" = drc::W1.4
            )
            fct_fn <- fct_map[[drc_fct]]
            if (is.null(fct_fn)) stop("Unknown drc family: ", drc_fct)
            drc::drm(formula, data = data, fct = fct_fn())
        },
        predict = function(model, newdata) {
            as.numeric(predict(model, newdata = newdata))
        },
        validate_classes = "drc"
    ))
}
}
\author{
Jacob Martin
}
