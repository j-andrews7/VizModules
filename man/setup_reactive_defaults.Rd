% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reactive_defaults.R
\name{setup_reactive_defaults}
\alias{setup_reactive_defaults}
\title{Resolve reactive \code{defaults} entries into a server-side parameter store}
\usage{
setup_reactive_defaults(defaults, input, session)
}
\arguments{
\item{defaults}{A named list of default values, or \code{NULL}. Individual entries
may be a \code{reactive()}/\code{reactiveVal}.}

\item{input}{The Shiny \code{input} object from inside \code{moduleServer()}.}

\item{session}{The Shiny \code{session} object from inside \code{moduleServer()}.}
}
\value{
\code{NULL} when \code{defaults} holds no reactive entries \u2014 in which case
modules behave exactly as they did before. Otherwise a list with two
functions, \code{has(key)} and \code{get(key)}, for \code{\link[=setup_auto_update_logic]{setup_auto_update_logic()}} to
read.
}
\description{
Plot module parameters normally travel through client-side inputs: the value
is seeded into a control by \verb{*InputsUI()} and read back at render time as
\verb{input$<key>}. That makes it impossible for a parent app to drive a parameter
from app state without a visible double render \u2014 \code{update*Input()} is an
asynchronous client round-trip, so the plot renders once with the stale value
and again when the new value arrives from the browser.
}
\details{
\code{setup_reactive_defaults()} fixes that by giving the module a \emph{server-side}
channel. Any \code{defaults} entry that is a \code{\link[shiny:reactive]{shiny::reactive()}} or
\code{\link[shiny:reactiveVal]{shiny::reactiveVal()}} is mirrored into an internal store that updates in the
same reactive flush as the parent's data, so the plot renders once. The store
is the render's source of truth (see \code{\link[=setup_auto_update_logic]{setup_auto_update_logic()}}); the
on-screen control is kept in sync separately and remains user-editable.

Semantics:
\itemize{
\item \strong{Precedence.} An external change always wins, overwriting a value
the user had typed into the control.
\item \strong{User edits.} Editing the control writes back to the store, so
manual overrides work exactly as they do for static defaults.
\item \strong{Reset.} The module's Reset button restores the reactive's
\emph{current} value, not the value it held at startup, because
\code{\link[=get_default]{get_default()}} resolves the reactive when the reset observer runs.
\item \strong{Recognised forms.} Only \code{reactive()} and \code{reactiveVal} are treated
as reactive defaults (via \code{\link[shiny:is.reactive]{shiny::is.reactive()}}). A plain function is
kept as a literal default value.
}

Control sync is cosmetic and uses a generic \code{sendInputMessage()}, which covers
the standard text, numeric, checkbox, select, colour and switch inputs. A
composite widget that ignores that message will simply not re-display the new
value; the plot is still correct, because the render reads the store.
}
\examples{
if (interactive()) {
    library(shiny)

    ui <- fluidPage(
        viz_select_input("sample", "Sample", c("S1", "S2", "S3")),
        dittoViz_scatterPlotInputsUI("p", mtcars),
        dittoViz_scatterPlotOutputUI("p")
    )

    server <- function(input, output, session) {
        # The plot title follows the selected sample, but stays editable.
        dittoViz_scatterPlotServer(
            "p",
            data = reactive(mtcars),
            defaults = list(main = reactive(input$sample))
        )
    }

    shinyApp(ui, server)
}
}
\seealso{
\code{\link[=setup_auto_update_logic]{setup_auto_update_logic()}}, \code{\link[=get_default]{get_default()}}
}
\author{
Jared Andrews
}
