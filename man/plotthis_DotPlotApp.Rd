% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotthis_DotPlot_module_app.R
\name{plotthis_DotPlotApp}
\alias{plotthis_DotPlotApp}
\title{Create an example Modular DotPlot Shiny Application}
\usage{
plotthis_DotPlotApp(data_list = NULL)
}
\arguments{
\item{data_list}{An optional named list of data frames. If \code{NULL} (the default),
\code{list("markers" = example_markers)} is used as example data.}
}
\value{
A Shiny app object.
}
\description{
This function generates a Shiny application with modular \code{\link[plotthis:DotPlot]{plotthis::DotPlot()}} components.
The app features a \strong{Data Import} section for uploading data,
a \strong{Data Table} for filtering the active dataset, and a \strong{Plot} area
for configuring and displaying an interactive dot plot.
}
\details{
When \code{data_list} is not provided (or \code{NULL}), the app launches with
\code{example_markers} as an example dataset. Uploaded data files are added
to the available datasets and can be selected for plotting. If an uploaded
file shares a name with an existing dataset, the existing one is overwritten
with a warning.

This is a convenience wrapper around \code{\link[=createModuleApp]{createModuleApp()}}.
}
\examples{
library(VizModules)
# Launch with default example data:
app <- plotthis_DotPlotApp()
if (interactive()) runApp(app)

# Launch with custom data:
app2 <- plotthis_DotPlotApp(list("markers" = example_markers))
if (interactive()) runApp(app2)
}
\author{
Jacob Martin, Jared Andrews
}
