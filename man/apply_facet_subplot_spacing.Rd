% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_facets.R
\name{apply_facet_subplot_spacing}
\alias{apply_facet_subplot_spacing}
\title{Apply custom subplot spacing to a faceted ggplotly figure}
\usage{
apply_facet_subplot_spacing(fig, spacing = 0.04, ncol = NULL, nrow = NULL)
}
\arguments{
\item{fig}{A plotly figure object (typically the result of \code{ggplotly()}).}

\item{spacing}{Numeric fraction of the plot area to leave between panels
(default \code{0.04}). May be a single value applied to both directions, or a
length-2 numeric vector \code{c(horizontal, vertical)} to control the gap
between columns and rows independently. Must satisfy
\code{horizontal * (ncol - 1) < 1} and \code{vertical * (nrow - 1) < 1}; otherwise
the figure is returned unchanged.}

\item{ncol}{Optional integer. Number of facet columns. If \code{NULL} or \code{NA},
detected from the number of distinct x-axis domain starts.}

\item{nrow}{Optional integer. Number of facet rows. If \code{NULL} or \code{NA},
detected from the number of distinct y-axis domain starts.}
}
\value{
The modified plotly figure with rewritten axis domains and
remapped paper-anchored annotations/shapes. Figures with a single panel
(or no layout) are returned unchanged.
}
\description{
\code{ggplotly()} assigns panel layout via plotly domain coordinates
(\verb{fig$x$layout$xaxis*/yaxis*$domain}) rather than honouring the ggplot2
\code{panel.spacing} theme option. This helper rewrites those domains so that
each facet panel has a uniform size and the gap between panels is exactly
\code{spacing} (expressed as a fraction of the plot area, e.g. \code{0.04}).
}
\details{
In addition to the axis domains, any paper-anchored layout annotations
(e.g. facet strip titles) and shapes (e.g. strip backgrounds, panel
borders) are remapped through a piecewise-linear transform built from the
old and new domain intervals, so strip labels and panel borders move with
their panels. Annotations/shapes whose \code{xref}/\code{yref} is tied to an axis
(for example \code{"x"}, \code{"y2"}, or \code{"x2 domain"}) are left alone because they
follow the rewritten axis automatically.

The number of columns and rows can be supplied manually, or detected
automatically from the distinct x / y domain starts already present in the
ggplotly output.
}
\examples{
p <- ggplot2::ggplot(mtcars, ggplot2::aes(wt, mpg)) +
    ggplot2::geom_point() +
    ggplot2::facet_wrap(~cyl)
fig <- plotly::ggplotly(p)
apply_facet_subplot_spacing(fig, spacing = 0.05)
}
\author{
Jacob Martin
}
