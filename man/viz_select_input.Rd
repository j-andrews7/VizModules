% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/select_inputs.R
\name{viz_select_input}
\alias{viz_select_input}
\title{Create a select input that scales to very large choice sets}
\usage{
viz_select_input(
  inputId,
  label,
  choices,
  selected = NULL,
  multiple = FALSE,
  search = NULL,
  width = "100\%",
  ...
)
}
\arguments{
\item{inputId}{The \code{input} slot that will be used to access the value.}

\item{label}{Display label for the control, or \code{NULL} for no label.}

\item{choices}{A vector or named list of values to select from, in the same
form accepted by \code{\link[shiny:selectInput]{shiny::selectInput()}}.}

\item{selected}{The initially selected value(s). Defaults to the first
choice for single selects and nothing for multiple selects.}

\item{multiple}{Logical, whether multiple values can be selected.}

\item{search}{Logical, whether to show a search box. Defaults to \code{NULL},
which enables the search box once there are more than 10 choices.}

\item{width}{The width of the input, e.g. \code{"100\%"} or \code{"400px"}.}

\item{...}{Further arguments passed to
\code{\link[shinyWidgets:virtualSelectInput]{shinyWidgets::virtualSelectInput()}}, including any raw \code{virtual-select}
property such as \code{optionsCount} or \code{zIndex}.}
}
\value{
A \code{shiny.tag} object to be included in a UI definition.
}
\description{
A drop-in replacement for \code{\link[shiny:selectInput]{shiny::selectInput()}} built on
\code{\link[shinyWidgets:virtualSelectInput]{shinyWidgets::virtualSelectInput()}}. The underlying \code{virtual-select}
library renders only the visible slice of the choice list, so a column with
tens of thousands of unique values stays responsive and readable instead of
producing an unusable wall of options.
}
\details{
Defaults are chosen so this behaves like \code{\link[shiny:selectInput]{shiny::selectInput()}}:
when \code{selected} is not supplied and \code{multiple = FALSE}, the first choice is
selected. An empty-string choice (the "no selection" convention used
throughout this package) is relabelled \code{"(none)"} so it is visible in the
dropdown, while its value remains \code{""}.
}
\examples{
library(VizModules)
viz_select_input("gene", "Gene", choices = c("", paste0("GENE", 1:1000)))
}
\seealso{
\code{\link[=update_viz_select]{update_viz_select()}}, \code{\link[shinyWidgets:virtualSelectInput]{shinyWidgets::virtualSelectInput()}}
}
\author{
Jared Andrews
}
