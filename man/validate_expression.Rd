% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse_utils.R
\name{validate_expression}
\alias{validate_expression}
\title{Validate a user-provided expression string for safety}
\usage{
validate_expression(expr_text, col_names)
}
\arguments{
\item{expr_text}{Character string containing the expression to validate
(e.g., \code{"group == 'A' & value > 10"}).}

\item{col_names}{Character vector of allowed column/symbol names
(typically \code{names(data)}).}
}
\value{
The original \code{expr_text} string if safe, or \code{NULL}.
}
\description{
Parses the expression text and walks the AST to ensure it only contains
allowed operations (comparisons, logical operators, column references, and
literals). Returns the original string if valid, or \code{NULL} if the input
is empty, unparseable, or contains disallowed operations. This is useful
when the expression string must be passed through to a downstream function
(e.g., \code{plotthis::BoxPlot(highlight = ...)}) rather than evaluated directly.
}
\details{
\strong{Use this when a module passes a user-typed expression string to an
external plotting function} that will evaluate it internally. The string
is validated but not executed by this function.
}
\examples{
validate_expression("Sepal.Length > 5", names(iris))
validate_expression("system('echo pwned')", names(iris)) # NULL + warning
validate_expression("", names(iris)) # NULL
}
\author{
Jared Andrews
}
