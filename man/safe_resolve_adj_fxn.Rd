% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse_utils.R
\name{safe_resolve_adj_fxn}
\alias{safe_resolve_adj_fxn}
\title{Safely resolve an adjustment function name to an actual function}
\usage{
safe_resolve_adj_fxn(fn_name)
}
\arguments{
\item{fn_name}{Character string — name of the adjustment function.
Currently allowed values: \code{"log2"}, \code{"log"}, \code{"log10"}, \code{"neg_log10"},
\code{"log1p"}, \code{"as.factor"}, \code{"abs"}, \code{"sqrt"}.}
}
\value{
The corresponding function, or \code{NULL} if \code{fn_name} is empty or
not in the allowed list.
}
\description{
Validates that the provided function name is in the allowed list before
converting it to a function reference. Returns \code{NULL} for empty strings
or unrecognized names.
}
\details{
\strong{Use this instead of \code{eval(str2expression())} when resolving function names
from user input} (e.g., a dropdown that selects a transformation like
\code{"log2"} or \code{"sqrt"}).
}
\examples{
safe_resolve_adj_fxn("log2")    # returns log2
safe_resolve_adj_fxn("")        # NULL
safe_resolve_adj_fxn("system")  # NULL + warning
}
\author{
Jared Andrews
}
