% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_mods.R
\name{collect_source_data}
\alias{collect_source_data}
\title{Collect plot and source data for download}
\usage{
collect_source_data(
  plot_reactive,
  stats_reactive = NULL,
  inputs_reactive = NULL
)
}
\arguments{
\item{plot_reactive}{A reactive expression returning a \code{plotly} plot object.}

\item{stats_reactive}{Optional. A reactive expression (e.g. a
\code{\link[shiny:reactiveVal]{shiny::reactiveVal()}}) returning a \code{data.frame} of statistical test results.
When \code{NULL} or when the reactive returns \code{NULL}, no statistics data is
included.}

\item{inputs_reactive}{Optional. A reactive expression returning a named
list of UI input values. When \code{NULL} or when it returns \code{NULL}, no UI
input data is included.}
}
\value{
A named list with elements:
\describe{
\item{plot}{The \code{plotly} plot object.}
\item{plot_data}{A \code{data.frame} of the plot's underlying data.}
\item{stats}{A \code{data.frame} of statistical test results, or \code{NULL}.}
\item{inputs}{A \code{data.frame} of UI input names and values, or \code{NULL}.}
}
}
\description{
Collects the plot object, its underlying data, statistical testing details (if applied),
and optional UI input values into a single list for downstream download
generation.
}
\examples{
\dontrun{
# Example usage in a Shiny app
library(shiny)
library(plotly)
library(VizModules)

ui <- fluidPage(
    plotlyOutput("my_plot"),
    downloadButton("download_data", "Download Plot and Data")
)

server <- function(input, output) {
    plot_reactive <- reactive({
       plot_ly(mtcars, x = ~mpg, y = ~hp, type = "scatter", mode = "markers")
    })

    data_list <- collect_source_data(plot_reactive)
    output$my_plot <- renderPlotly(plot_reactive())
    output$download_data <- create_source_download_handler(reactive(data_list))
}

shinyApp(ui, server)
}
}
\author{
Jacob Martin
}
