% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stat_helper.R
\name{compute_pairwise_stats}
\alias{compute_pairwise_stats}
\title{Compute pairwise statistical tests between groups}
\usage{
compute_pairwise_stats(
  df,
  x,
  y,
  pairs = NULL,
  test = "wilcox.test",
  p.adjust.method = "holm",
  paired = FALSE,
  group.by = NULL,
  facet.by = NULL,
  per.facet = TRUE,
  sig.threshold = 0.05,
  sig.levels = c(`****` = 1e-04, `***` = 0.001, `**` = 0.01)
)
}
\arguments{
\item{df}{Data frame containing the data.}

\item{x}{Character; column name of the categorical x-axis variable.}

\item{y}{Character; column name of the numeric response variable.}

\item{pairs}{List of length-2 character vectors specifying group pairs to test.
If NULL (default), tests all unique pairwise combinations.}

\item{test}{Character; statistical test to use. One of \code{"wilcox.test"},
\code{"t.test"}, \code{"kruskal.test"}, or \code{"anova"}.}

\item{p.adjust.method}{Character; method for p-value adjustment via
\code{\link[stats:p.adjust]{stats::p.adjust()}}. Default \code{"holm"}.}

\item{paired}{Logical; whether to perform paired tests (only for
\code{"wilcox.test"} and \code{"t.test"}). Default FALSE.}

\item{group.by}{Character or NULL; column for nested grouping. When set,
comparisons are made between levels of \code{group.by} within each level of \code{x}.}

\item{facet.by}{Character or NULL; column for faceting. When set and
\code{per.facet = TRUE}, tests run independently per facet panel.}

\item{per.facet}{Logical; if TRUE and \code{facet.by} is set, run tests
independently per facet panel. Default TRUE.}

\item{sig.threshold}{Numeric; significance threshold for \code{*} vs \code{ns}.
P-values at or below this are labeled \code{*}; above are labeled \code{ns}. Default 0.05.
See \code{sig.levels} for the multi-star thresholds.}

\item{sig.levels}{Named numeric vector; upper p-value bounds for multi-star
significance symbols. Names are the displayed symbols and values are the
thresholds. Default \code{c("****" = 0.0001, "***" = 0.001, "**" = 0.01)}.
Any number of levels can be provided. Evaluated from smallest to largest
threshold so the most significant symbol always wins.}
}
\value{
A data.frame with columns: \code{group1}, \code{group2}, \code{p.value}, \code{p.adj},
\code{p.signif}, \code{test}, \code{facet_level}, \code{x_level} (when \code{group.by} is set).
}
\description{
Performs pairwise statistical tests between groups defined by a categorical
variable. Supports Wilcoxon rank-sum, t-test, Kruskal-Wallis, and ANOVA.
Handles nested grouping (comparing color.by groups within each x-level)
and per-facet testing.
}
\examples{
compute_pairwise_stats(
    df = example_iris,
    x = "Species",
    y = "Sepal.Length",
    test = "wilcox.test"
)

# Custom significance levels: only two-star tiers, lower threshold for *
compute_pairwise_stats(
    df = example_iris,
    x = "Species",
    y = "Sepal.Length",
    test = "wilcox.test",
    sig.threshold = 0.01,
    sig.levels = c("**" = 0.001, "***" = 0.0001)
)

}
\author{
Jared Andrews, Jacob Martin
}
