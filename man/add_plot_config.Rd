% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_helpers.R
\name{add_plot_config}
\alias{add_plot_config}
\title{Create default Plotly configuration}
\usage{
add_plot_config(
  download.format = "png",
  filename = as.character(Sys.Date()),
  include.modebar.buttons = TRUE,
  facet.by = NULL
)
}
\arguments{
\item{download.format}{Character. The image format for downloads (e.g., "png", "svg", "jpeg").}

\item{filename}{Character. The filename for downloaded images (default: current date).}

\item{include.modebar.buttons}{Logical. Whether to include drawing tool buttons in the modebar (default: TRUE).}

\item{facet.by}{Logical. Whether the figure is facetted to determine if axes labels for each plot should be editable or not.}
}
\value{
A named list suitable for use as the \code{config} argument in Plotly
calls, containing edit options, image download settings, extra modebar
buttons, and logo display preferences.
}
\description{
Constructs a configuration list for Plotly plots, enabling interactive
editing of titles and legends, export options, and additional drawing tools
in the modebar.
}
\details{
The configuration enables interactive editing of the
plot title, legend text and position, colorbar position and title, and
annotation tails. It also adds drawing tools (lines, paths, circles,
rectangles, and an eraser) to the modebar. Native cartesian axis-title
text editing is disabled because axis titles are rendered as draggable,
editable annotations (see \code{\link[=axis_titles_as_annotations]{axis_titles_as_annotations()}} and
\code{\link[=.build_facet_annotations]{.build_facet_annotations()}}).
}
\examples{
add_plot_config()
add_plot_config(download.format = "svg", include.modebar.buttons = FALSE)
}
\author{
Jacob Martin
}
