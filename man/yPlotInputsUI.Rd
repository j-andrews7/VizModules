% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/yPlot_module_ui.R
\name{yPlotInputsUI}
\alias{yPlotInputsUI}
\title{Input UI components for the yPlot module}
\usage{
yPlotInputsUI(id, data, defaults = NULL, title = NULL, columns = 2)
}
\arguments{
\item{id}{The ID for the Shiny module.}

\item{data}{The data frame used for plot generation.}

\item{defaults}{A named list of default values for the inputs.}

\item{title}{An optional title for the UI grid.}

\item{columns}{Number of columns for the UI grid.}
}
\value{
A Shiny tagList containing the UI elements
}
\description{
This should be placed in the UI where the inputs should be shown, with an \code{id}
that matches the \code{id} used in the \code{yPlotServer()} and \code{yPlotOutputUI()} functions.
}
\details{
The user inputs for this module are separated from the outputs to allow for
more flexible UI design.

The inputs will automatically be organized into a grid layout via the \code{organize_inputs()} function,
with \code{columns} controlling the number of columns in the grid.

Defaults can be set for each input by providing a named list of values to the \code{defaults} argument.
Nearly all parameters for \code{\link[dittoViz:yPlot]{dittoViz::yPlot()}} can be set via these inputs, so see the help
for that function for an exhaustive list.
}
\examples{
library(vizModules)
data(mtcars)
yPlotInputsUI("yPlot", mtcars)
}
\seealso{
\code{\link[dittoViz:yPlot]{dittoViz::yPlot()}}, \code{\link[=organize_inputs]{vizModules::organize_inputs()}},
\code{\link[=yPlotOutputUI]{vizModules::yPlotOutputUI()}}, \code{\link[=yPlotServer]{vizModules::yPlotServer()}}, \code{\link[=yPlotApp]{vizModules::yPlotApp()}}
}
\author{
Jared Andrews
}
