% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{example_composition}
\alias{example_composition}
\title{Example single-cell-style composition data for the freqPlot module}
\format{
A data frame with 1800 rows and 7 columns:
\describe{
\item{cell_id}{Unique cell identifier (character)}
\item{sample}{Donor identifier, \code{P01}-\code{P12} (factor); 150 cells each}
\item{condition}{Disease state, \code{Healthy} or \code{Disease} (factor); six donors each}
\item{batch}{Processing batch, \code{B1} or \code{B2} (factor); crossed with \code{condition}}
\item{cell_type}{Annotated cell type (factor), the variable whose
per-sample frequency \code{freqPlot()} tabulates}
\item{n_genes}{Number of genes detected in the cell (integer)}
\item{percent_mito}{Percentage of mitochondrial reads (numeric)}
}
}
\source{
Simulated. Per-donor compositions are Dirichlet draws around
condition-specific means, with cell counts drawn multinomially.
See \code{data-raw/generate_example_data.R}.
}
\usage{
example_composition
}
\description{
A per-cell record table from a simulated 12-donor immune profiling
experiment, shaped for \code{\link[dittoViz:freqPlot]{dittoViz::freqPlot()}}. Each donor (\code{sample})
contributes 150 cells and maps to exactly one \code{condition} and one \code{batch},
which is the nesting \code{freqPlot()} requires to compare per-sample
cell-type frequencies across groups. \code{batch} is crossed with \code{condition}
(three donors each), so it works as a \code{color.by} without confounding the
comparison.
}
\details{
Composition differs between the two conditions: the Disease donors show an
expanded monocyte compartment and depleted CD4 T cells relative to Healthy.
}
\author{
Jared Andrews
}
\keyword{datasets}
