% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotthis_ViolinPlot_module_app.R
\name{plotthis_ViolinPlotApp}
\alias{plotthis_ViolinPlotApp}
\title{Create an example Modular ViolinPlot Shiny Application}
\usage{
plotthis_ViolinPlotApp(data_list = NULL)
}
\arguments{
\item{data_list}{An optional named list of data frames. If \code{NULL} (the default),
\code{list("demographics" = example_demographics)} is used as example data.}
}
\value{
A Shiny app object.
}
\description{
This function generates a Shiny application with modular \code{\link[plotthis:ViolinPlot]{plotthis::ViolinPlot()}} components.
The app features a \strong{Data Import} section for uploading data,
a \strong{Data Table} for filtering the active dataset, and a \strong{Plot} area
for configuring and displaying an interactive violin plot.
}
\details{
When \code{data_list} is not provided (or \code{NULL}), the app launches with
\code{example_demographics} as an example dataset. Uploaded data files are added
to the available datasets and can be selected for plotting. If an uploaded
file shares a name with an existing dataset, the existing one is overwritten
with a warning.

This is a convenience wrapper around \code{\link[=createModuleApp]{createModuleApp()}}.
}
\examples{
library(VizModules)
# Launch with default example data:
app <- plotthis_ViolinPlotApp()
if (interactive()) runApp(app)

# Launch with custom data:
app2 <- plotthis_ViolinPlotApp(list("demographics" = example_demographics))
if (interactive()) runApp(app2)
}
\author{
Jacob Martin, Jared Andrews
}
