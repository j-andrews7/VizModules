% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse_utils.R
\name{safe_eval_filter}
\alias{safe_eval_filter}
\title{Safely evaluate a user-provided filter expression against a data frame}
\usage{
safe_eval_filter(expr_text, data)
}
\arguments{
\item{expr_text}{Character string containing the filter expression
(e.g., \code{"Sepal.Length > 5 & Species == 'setosa'"}).}

\item{data}{A \code{data.frame} whose columns are made available for the expression.}
}
\value{
A logical vector the same length as \code{nrow(data)}, or \code{NULL} if the
input is empty, unparseable, or contains disallowed operations.
}
\description{
Parses the expression text, validates that it only contains allowed
operations (comparisons, logical operators, column references, and
literals), then evaluates it in a restricted environment containing
only the data frame columns. Returns a logical vector suitable for
row subsetting, or \code{NULL} if the input is empty or invalid.
}
\details{
\strong{Use this function any time a module evaluates a user-typed expression
directly} (e.g., a row-filter text input). Never call \code{eval(str2expression())}
on raw user input — doing so allows arbitrary code execution on the server.
}
\examples{
safe_eval_filter("Sepal.Length > 5", iris)
safe_eval_filter("Sepal.Length > 5 & Species == 'setosa'", iris)
safe_eval_filter("", iris) # NULL
safe_eval_filter("system('echo pwned')", iris) # NULL + warning
}
\author{
Jared Andrews
}
