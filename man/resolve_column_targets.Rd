% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dataFilter_module_server.R
\name{resolve_column_targets}
\alias{resolve_column_targets}
\title{Translate column names or positions into DT column targets}
\usage{
resolve_column_targets(data, columns, rownames = FALSE)
}
\arguments{
\item{data}{A data frame, or a character vector of the column names in the
order they are passed to \code{\link[DT:datatable]{DT::datatable()}}.}

\item{columns}{\code{NULL}, a character vector of column names, or a numeric
vector of one-based column positions to resolve.}

\item{rownames}{Logical. Whether the table is drawn with a row-names column
(the \code{rownames} argument of \code{\link[DT:datatable]{DT::datatable()}}). When \code{TRUE}, row names
occupy column 0 and every data column shifts one to the right, so the
returned targets are shifted to match. Defaults to \code{FALSE}.}
}
\value{
An integer vector of zero-based column indices, possibly empty.
}
\description{
Maps a set of columns onto the zero-based \code{targets} indices DataTables
expects inside a \code{columnDefs} entry. This is what \code{\link[=dataFilterServer]{dataFilterServer()}} uses
to honour its \code{hide.columns} argument, but it is useful for any hand-rolled
\code{\link[DT:datatable]{DT::datatable()}} where columns are referred to by name rather than by
position -- hiding them, setting widths, disabling ordering, and so on.
}
\details{
Columns that do not exist are dropped with a warning rather than raising an
error, so a table fed by a changing data frame keeps rendering when a column
comes and goes.
}
\examples{
# Hide two columns of a plain DT table by name.
targets <- resolve_column_targets(iris, c("Petal.Length", "Petal.Width"))
targets

if (interactive()) {
    DT::datatable(
        iris,
        rownames = FALSE,
        options = list(
            columnDefs = list(list(visible = FALSE, targets = targets))
        )
    )
}
}
\seealso{
\code{\link[=dataFilterServer]{dataFilterServer()}}
}
\author{
Jared Andrews
}
