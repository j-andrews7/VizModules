% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parallelCoordinatesPlot_module_app.R
\name{parallelCoordinatesPlotApp}
\alias{parallelCoordinatesPlotApp}
\title{Create an example Modular parallelCoordinatesPlot Shiny Application}
\usage{
parallelCoordinatesPlotApp(data_list = NULL)
}
\arguments{
\item{data_list}{An optional named list of data frames. If \code{NULL} (the default),
\code{list("mtcars" = example_mtcars, "iris" = example_iris)} is used as example data. Each data frame
should contain at least two numeric or categorical columns.}
}
\value{
A Shiny app object.
}
\description{
This function generates a Shiny application with modular
\code{\link[=parallelCoordinatesPlot]{parallelCoordinatesPlot()}} components.
The app features a \strong{Data Import} section for uploading data,
a \strong{Data Table} for filtering the active dataset, and a \strong{Plot} area
for configuring and displaying an interactive parallel coordinates plot.
}
\details{
When \code{data_list} is not provided (or \code{NULL}), the app launches with
\code{example_mtcars} and \code{example_iris} as example datasets. Uploaded data files are added
to the available datasets and can be selected for plotting. If an uploaded
file shares a name with an existing dataset, the existing one is overwritten
with a warning.

This is a convenience wrapper around \code{\link[=createModuleApp]{createModuleApp()}}.
}
\examples{
library(VizModules)
# Launch with default example data:
app <- parallelCoordinatesPlotApp()
if (interactive()) runApp(app)

# Launch with custom data:
app2 <- parallelCoordinatesPlotApp(list("sales" = example_sales, "population" = example_population))
if (interactive()) runApp(app2)
}
\seealso{
\code{\link[=parallelCoordinatesPlot]{parallelCoordinatesPlot()}}, \code{\link[=parallelCoordinatesPlotInputsUI]{parallelCoordinatesPlotInputsUI()}},
\code{\link[=parallelCoordinatesPlotOutputUI]{parallelCoordinatesPlotOutputUI()}}, \code{\link[=parallelCoordinatesPlotServer]{parallelCoordinatesPlotServer()}}
}
\author{
Jacob Martin, Jared Andrews
}
