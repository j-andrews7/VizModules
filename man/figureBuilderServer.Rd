% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/figureBuilder_module_server.R
\name{figureBuilderServer}
\alias{figureBuilderServer}
\title{Server logic for the Figure Builder module}
\usage{
figureBuilderServer(id, data_list = NULL, module_registry = NULL)
}
\arguments{
\item{id}{The ID for the Shiny module. Must match the \code{id} given to
\code{\link[=figureBuilderUI]{figureBuilderUI()}}.}

\item{data_list}{An optional named list of data frames that seed the dataset
registry. If \code{NULL} (the default), the bundled example datasets (plus a
\code{sales_by_product} summary suited to the pie plot) are used. At least one
element is required and every element must be a data frame.}

\item{module_registry}{An optional named list describing the plot modules to
offer. If \code{NULL} (the default), all bundled VizModules modules are offered.
Each entry is itself a list with components: \code{label} (character, shown in the
picker), \code{dataset} (character, the dataset name its \code{defaults} were written
for), \code{inputs_ui}, \code{output_ui}, and \code{server_fn} (the module's three
functions), and \code{defaults} (a named list of input defaults applied only when
\code{dataset} is the chosen dataset).}
}
\value{
Invisibly returns \code{NULL}; called for its side effects (wiring up the
Figure Builder module's reactive logic).
}
\description{
Powers the multi-panel \strong{Figure Builder} module rendered by
\code{\link[=figureBuilderUI]{figureBuilderUI()}}. Users can add any VizModules plot module to a free-form
A4 canvas, drag and resize each plot, filter each plot's data independently,
label panels automatically, and export the whole figure as a single editable
SVG (or bundle every plot's source data, HTML plot, and statistics into one
\code{.zip}).
}
\details{
Call this from your app's server with the same \code{id} you passed to
\code{\link[=figureBuilderUI]{figureBuilderUI()}}. Because it is a proper Shiny module, several Figure
Builders can coexist on one page, each with its own namespace and canvas.
}
\examples{
library(VizModules)
if (interactive()) {
    ui <- fluidPage(figureBuilderUI("figure_builder"))
    server <- function(input, output, session) {
        figureBuilderServer("figure_builder")
    }
    shinyApp(ui, server)
}
}
\seealso{
\code{\link[=figureBuilderUI]{figureBuilderUI()}}, \code{\link[=figureBuilderApp]{figureBuilderApp()}}
}
\author{
Jared Andrews
}
