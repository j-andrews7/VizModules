% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/linePlot_module_app.R
\name{linePlotApp}
\alias{linePlotApp}
\title{Create an example Modular linePlot Shiny Application}
\usage{
linePlotApp(data_list = NULL)
}
\arguments{
\item{data_list}{An optional named list of data frames. If \code{NULL} (the default),
\code{list("sales" = gallery_sales)} is used as example data.}
}
\value{
A Shiny app object.
}
\description{
This function generates a Shiny application with modular \code{\link[=linePlot]{linePlot()}} components.
The app features a \strong{Data Import} section for uploading data,
a \strong{Data Table} for filtering the active dataset, and a \strong{Plot} area
for configuring and displaying an interactive line plot.
}
\details{
When \code{data_list} is not provided (or \code{NULL}), the app launches with
\code{gallery_sales} as an example dataset. Uploaded data files are added
to the available datasets and can be selected for plotting. If an uploaded
file shares a name with an existing dataset, the existing one is overwritten
with a warning.

This is a convenience wrapper around \code{\link[=createModuleApp]{createModuleApp()}}.
}
\examples{
library(VizModules)
# Launch with default example data (gallery_sales):
app <- linePlotApp()
if (interactive()) runApp(app)

# Launch with custom data:
app2 <- linePlotApp(list("sales" = gallery_sales))
if (interactive()) runApp(app2)
}
\seealso{
\code{\link[=linePlot]{linePlot()}}, \code{\link[=linePlotInputsUI]{linePlotInputsUI()}},
\code{\link[=linePlotOutputUI]{linePlotOutputUI()}}, \code{\link[=linePlotServer]{linePlotServer()}}
}
\author{
Jacob Martin, Jared Andrews
}
