% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ternaryPlot_module_app.R
\name{ternaryPlotApp}
\alias{ternaryPlotApp}
\title{Create an example Modular ternaryPlot Shiny Application}
\usage{
ternaryPlotApp(data_list = NULL)
}
\arguments{
\item{data_list}{An optional named list of data frames. If \code{NULL} (the default),
\code{list("roles" = example_roles)} is used. Each data frame should
contain numeric columns for the three ternary axes (a, b, c). For multiple traces,
include a grouping column.}
}
\value{
A Shiny app object.
}
\description{
This function generates a Shiny application with modular ternaryPlot components.
The app features a \strong{Data Import} section for uploading data,
a \strong{Data Table} for filtering the active dataset, and a \strong{Plot} area
for configuring and displaying an interactive ternary plot.
}
\details{
When \code{data_list} is not provided (or \code{NULL}), the app launches with
\code{example_roles} as an example dataset. Uploaded data files are added
to the available datasets and can be selected for plotting. If an uploaded
file shares a name with an existing dataset, the existing one is overwritten
with a warning.

This is a convenience wrapper around \code{\link[=createModuleApp]{createModuleApp()}}.
}
\examples{
library(VizModules)
# Launch with default example data:
app <- ternaryPlotApp()
if (interactive()) runApp(app)

# Launch with custom data:
df <- data.frame(
    journalist = c(75, 70, 75, 5, 10),
    developer = c(25, 10, 20, 60, 80),
    designer = c(0, 20, 5, 35, 10)
)
app2 <- ternaryPlotApp(list("roles" = df))
if (interactive()) runApp(app2)
}
\seealso{
\code{\link[=ternaryPlot]{ternaryPlot()}}, \code{\link[=ternaryPlotInputsUI]{ternaryPlotInputsUI()}},
\code{\link[=ternaryPlotOutputUI]{ternaryPlotOutputUI()}}, \code{\link[=ternaryPlotServer]{ternaryPlotServer()}}
}
\author{
Jacob Martin, Jared Andrews
}
