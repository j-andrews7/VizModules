% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stat_helper.R
\name{plot_stats}
\alias{plot_stats}
\title{Add Statistical Test Annotations to Plotly Figure}
\usage{
plot_stats(
  fig,
  df,
  x,
  y,
  pairs = NULL,
  order = NULL,
  type_test = "wilcox.test",
  type_correction = NULL,
  p_adjustment = "holm",
  cutoff_pvalue = 0.05
)
}
\arguments{
\item{fig}{A plotly object to annotate}

\item{df}{Dataframe containing the data}

\item{x}{Character name of the categorical grouping column (e.g., "condition")}

\item{y}{Character name of the numeric response column (e.g., "expression")}

\item{pairs}{List of length-2 vectors specifying which group pairs to test.
If NULL (default), tests all unique pairwise combinations from x column.}

\item{order}{Character vector specifying desired x-axis group order.
If NULL (default), uses natural order of unique values in x.}

\item{type_test}{Character; currently only "wilcox" (Mann-Whitney U test) supported}

\item{type_correction}{Character; reserved for future multiple testing correction}

\item{cutoff_pvalue}{Numeric (0.05); currently unused but reserved for filtering}
}
\value{
Annotated plotly figure with p-value labels positioned above bars/groups
}
\description{
Automatically performs pairwise statistical tests (Wilcoxon rank-sum by default)
between groups in a categorical X column, calculates p-values, and adds
significance annotations above the plot area positioned according to group order.
}
\details{
Performs Wilcoxon rank-sum tests between all specified pairs (or all pairs if
\code{pairs = NULL}). P-value annotations appear above the plot in red text,
horizontally centered between the tested group pair, vertically spaced by 10\%
of y-range increments. Y-axis automatically expands to accommodate annotations.

Group order validation ensures all specified \code{order} elements exist in x column
and match its unique length. Early return with message on validation failure.

Designed for bioinformatics visualization workflows - e.g., gene expression
across conditions, cell type comparisons.
}
\author{
Jacob
}
