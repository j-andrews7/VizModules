% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_reference_lines.R
\name{add_reference_lines}
\alias{add_reference_lines}
\title{Add reference lines to a plotly figure from Shiny inputs}
\usage{
add_reference_lines(
  fig,
  hline.intercepts = NULL,
  hline.colors = NULL,
  hline.widths = NULL,
  hline.linetypes = NULL,
  hline.opacities = NULL,
  vline.intercepts = NULL,
  vline.colors = NULL,
  vline.widths = NULL,
  vline.linetypes = NULL,
  vline.opacities = NULL,
  abline.slopes = NULL,
  abline.intercepts = NULL,
  abline.colors = NULL,
  abline.widths = NULL,
  abline.linetypes = NULL,
  abline.opacities = NULL
)
}
\arguments{
\item{fig}{A plotly figure object.}

\item{hline.intercepts}{Character. Comma-separated y-intercepts for horizontal lines.}

\item{hline.colors}{Character. Comma-separated colors for horizontal lines.}

\item{hline.widths}{Character. Comma-separated widths for horizontal lines.}

\item{hline.linetypes}{Character. Comma-separated linetypes for horizontal lines.}

\item{hline.opacities}{Character. Comma-separated opacities for horizontal lines.}

\item{vline.intercepts}{Character. Comma-separated x-intercepts for vertical lines.}

\item{vline.colors}{Character. Comma-separated colors for vertical lines.}

\item{vline.widths}{Character. Comma-separated widths for vertical lines.}

\item{vline.linetypes}{Character. Comma-separated linetypes for vertical lines.}

\item{vline.opacities}{Character. Comma-separated opacities for vertical lines.}

\item{abline.slopes}{Character. Comma-separated slopes for diagonal lines.}

\item{abline.intercepts}{Character. Comma-separated y-intercepts for diagonal lines.}

\item{abline.colors}{Character. Comma-separated colors for diagonal lines.}

\item{abline.widths}{Character. Comma-separated widths for diagonal lines.}

\item{abline.linetypes}{Character. Comma-separated linetypes for diagonal lines.}

\item{abline.opacities}{Character. Comma-separated opacities for diagonal lines.}
}
\value{
The modified plotly figure with all specified lines added.
}
\description{
Convenience wrapper that adds horizontal, vertical, and/or diagonal lines
to a plotly figure based on parsed Shiny input values.
}
\examples{
fig <- plotly::plot_ly(mtcars, x = ~wt, y = ~mpg, type = "scatter", mode = "markers")
add_reference_lines(fig,
    hline.intercepts = "20, 30", hline.colors = "red, blue",
    vline.intercepts = "3", abline.slopes = "5", abline.intercepts = "0"
)
}
\author{
Jared Andrews
}
