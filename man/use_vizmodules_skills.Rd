% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/use_skills.R
\name{use_vizmodules_skills}
\alias{use_vizmodules_skills}
\title{Install the bundled VizModules agent skills into a project}
\usage{
use_vizmodules_skills(path = ".", overwrite = FALSE)
}
\arguments{
\item{path}{Directory of the project to install into. The skills are written to
\code{file.path(path, ".claude", "skills")}, which is created if needed.}

\item{overwrite}{Logical; if \code{FALSE} (the default) a skill whose directory
already exists is skipped rather than replaced.}
}
\value{
Invisibly, a character vector of the skill directories written.
}
\description{
Copies the skills that ship with VizModules into a project's \verb{.claude/skills/}
directory, where Claude Code and other tools following the
\href{https://agentskills.io}{Agent Skills} convention will discover them.
}
\details{
Three skills are provided:

\describe{
\item{\code{vizmodules-app}}{Wiring plot modules into a Shiny app: \code{defaults},
\code{hide.inputs}/\code{hide.tabs}, the Stats tab, \code{\link[=createModuleApp]{createModuleApp()}}, the data
filter table, the figure builder, and source-data export. Ships a generated
inventory of every module's column-mapping keys, colour key, and tab names.}
\item{\code{vizmodules-custom-module}}{Building a wrapper module on top of a base
module: the namespace contract, reactive \code{defaults}, avoiding double
renders, manual-edit persistence, and model-line backends.}
\item{\code{vizmodules-new-module}}{Authoring a module inside this package: the
file trio, the required roxygen sections, the uniform input helpers, and
file templates to copy.}
}
}
\examples{
# Install into a temporary project rather than the current one:
use_vizmodules_skills(tempdir())

\dontrun{
# Install into the current project, refreshing any that already exist:
use_vizmodules_skills(".", overwrite = TRUE)
}
}
\author{
Jared Andrews
}
