% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ui_utils.R
\name{showInput}
\alias{showInput}
\title{Show the grid cells wrapping module inputs}
\usage{
showInput(session, ids)
}
\arguments{
\item{session}{The module \code{session} object (provides \code{session$ns}).}

\item{ids}{Character vector of un-namespaced input IDs to toggle.}
}
\value{
Invisibly \code{NULL}, called for the side effect of running client-side JS.
}
\description{
Toggles the visibility of the \code{.vizmodules-input-cell} that wraps a given input
(as laid out by \code{\link[=organize_inputs]{organize_inputs()}}). Hiding the cell rather than just the input
itself lets the surrounding inputs reflow so the panel stays compact instead of
leaving an empty gap, as a plain \code{shinyjs::hide()} on the input would.
}
\examples{
\dontrun{
# Call inside a module server:
showInput(session, c("size", "opacity"))
}
}
\seealso{
\code{\link[=hideInput]{hideInput()}}, \code{\link[=toggleInputCell]{toggleInputCell()}}, \code{\link[=organize_inputs]{organize_inputs()}}
}
\author{
Jared Andrews
}
