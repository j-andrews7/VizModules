% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_axis.R
\name{create_ggplot_axis_style}
\alias{create_ggplot_axis_style}
\title{Create ggplot axis styling theme arguments}
\usage{
create_ggplot_axis_style(input, isolate_fn = isolate)
}
\arguments{
\item{input}{Shiny input object containing axis styling parameters.}

\item{isolate_fn}{Function to use for isolating reactive values (default: isolate).}
}
\value{
A named list of ggplot2 theme arguments to be passed to theme_args parameter.
}
\description{
Creates ggplot2 theme arguments for axis borders and lines based on user inputs.
This function handles axis styling through ggplot2 themes rather than plotly overlays,
which provides better control especially when faceting is used.
}
\details{
When faceting is enabled, panel borders are always shown for the full plot.
When faceting is disabled:
\itemize{
\item If both axis.showline and axis.mirror are TRUE: Full panel border
\item If only axis.showline is TRUE: Axis lines on x and y axes only
\item Otherwise: No borders
}
}
\examples{
input <- list(
    axis.showline = TRUE, axis.mirror = TRUE,
    axis.linecolor = "black", axis.linewidth = 1
)
create_ggplot_axis_style(input, isolate_fn = identity)
}
\author{
Jacob Martin
}
