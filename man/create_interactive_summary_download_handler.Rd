% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_mods.R
\name{create_interactive_summary_download_handler}
\alias{create_interactive_summary_download_handler}
\title{Create download handler for an interactive plot summary}
\usage{
create_interactive_summary_download_handler(
  plot_reactive,
  stats_reactive = NULL,
  inputs_reactive = NULL,
  filename_base = "interactive_summary"
)
}
\arguments{
\item{plot_reactive}{A reactive expression returning a \code{plotly} plot object.}

\item{stats_reactive}{Optional. A reactive expression (e.g. a
\code{\link[shiny:reactiveVal]{shiny::reactiveVal()}}) returning a \code{data.frame} of summary statistics
to include in the archive. When \code{NULL} or when the reactive returns
\code{NULL}, \code{stats_data.csv} is omitted from the zip.}

\item{inputs_reactive}{Optional. A reactive expression returning a named
list of UI input values (typically built with
\code{\link[shiny:reactiveValuesToList]{shiny::reactiveValuesToList()}} on the module's \code{input}). When \code{NULL}
or empty, \code{ui_inputs.csv} is omitted from the zip.}

\item{filename_base}{\code{character(1)}. Base name for the downloaded \code{.zip}
file (without extension). The final filename takes the form
\verb{<filename_base>_<Sys.Date()>.zip}. Defaults to \code{"interactive_summary"}.}
}
\value{
A \code{downloadHandler} object suitable for direct assignment to a
Shiny output (e.g. \code{output$download.interactive.summary}). The resulting
\code{.zip} archive contains:
\describe{
\item{\code{plot.html}}{Self-contained interactive plotly HTML widget.}
\item{\code{plot_Data.csv}}{CSV of the data underlying the plot, as
returned by \code{\link[plotly:plotly_data]{plotly::plotly_data()}}.}
\item{\code{stats_data.csv}}{CSV of the statistics summary (only present
when \code{stats_reactive} is non-\code{NULL} and returns non-empty data).}
\item{\code{ui_inputs.csv}}{CSV of the current UI input names and values
(only present when \code{inputs_reactive} is non-\code{NULL} and returns a
non-empty list).}
}
}
\description{
Generates a Shiny \code{\link[=downloadHandler]{downloadHandler()}} that bundles the interactive plot
and its supporting data into a single \code{.zip} archive. The archive contains
a self-contained interactive HTML of the plotly plot (rendered via
\code{\link[htmlwidgets:saveWidget]{htmlwidgets::saveWidget()}}), a CSV of the plot's underlying data
(obtained via \code{\link[plotly:plotly_data]{plotly::plotly_data()}}), and, when supplied, a CSV of the
statistics summary and a CSV snapshot of the current UI input values.
Used by all VizModules to provide the "Interactive Summary" download
button beside the standard control panel buttons (Auto Update / Update /
Reset).
}
\examples{
if (interactive()) {
    library(shiny)
    library(plotly)

    ui <- fluidPage(
        plotlyOutput("plot"),
        downloadButton("download_summary", "Download Summary")
    )

    server <- function(input, output, session) {
        # A reactive plotly plot
        plot_reactive <- reactive({
            plot_ly(mtcars, x = ~wt, y = ~mpg, type = "scatter", mode = "markers")
        })

        # Optional: a reactive returning a stats data.frame
        stats_reactive <- reactive({
            data.frame(
                metric = c("mean_mpg", "sd_mpg"),
                value  = c(mean(mtcars$mpg), sd(mtcars$mpg))
            )
        })

        # Optional: capture all UI inputs as a named list
        AllInputs <- reactive({
            reactiveValuesToList(input)
        })

        output$plot <- renderPlotly(plot_reactive())

        output$download_summary <- create_interactive_summary_download_handler(
            plot_reactive   = plot_reactive,
            stats_reactive  = stats_reactive,
            inputs_reactive = AllInputs(),
            filename_base   = "my_plot_summary"
        )
    }

    shinyApp(ui, server)
}

}
\author{
Jacob Martin
}
