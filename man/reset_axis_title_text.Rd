% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_helpers.R
\name{reset_axis_title_text}
\alias{reset_axis_title_text}
\title{Drop persisted axis-title text edits}
\usage{
reset_axis_title_text(store, keys = c("axis:x", "axis:y"))
}
\arguments{
\item{store}{The list returned by \code{\link[=setup_manual_edits]{setup_manual_edits()}}.}

\item{keys}{Character vector of axis annotation keys to clear text for.
Defaults to both axis titles, \code{c("axis:x", "axis:y")}. The
\verb{#<occurrence>} suffix added by \code{\link[=.annotation_edit_keys]{.annotation_edit_keys()}} is ignored when
matching.}
}
\value{
Invisibly, \code{TRUE} if any stored text was removed, otherwise \code{FALSE}.
}
\description{
Removes any captured \code{text} edit for the given axis-title annotation keys from
an edit store, so the axis title is regenerated from its (possibly adjusted)
data-derived label on the next rebuild rather than restoring a stale manual
edit. Any captured position for those titles is left intact, so a dragged
title keeps its place. Intended to be called when the plotted variable for an
axis changes (e.g. from an \code{observeEvent()} or inside the plot-building
reactive), matching the convention that a manual title only makes sense for
the variable it was written for.
}
\examples{
\dontrun{
# Regenerate the y-axis title whenever the plotted variable changes:
observeEvent(input$var, reset_axis_title_text(edit_store, "axis:y"),
    ignoreInit = TRUE)
}
}
\seealso{
\code{\link[=setup_manual_edits]{setup_manual_edits()}}, \code{\link[=finalize_manual_edits]{finalize_manual_edits()}}.
}
\author{
Jared Andrews
}
