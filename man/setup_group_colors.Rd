% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse_utils.R
\name{setup_group_colors}
\alias{setup_group_colors}
\title{Track the group-to-color mapping a plot should draw with}
\usage{
setup_group_colors(
  input,
  key,
  groups,
  default_palette = NULL,
  defaults = NULL,
  params = NULL
)
}
\arguments{
\item{input}{The Shiny \code{input} object from inside \code{moduleServer()}.}

\item{key}{Character string — the picker's input id, without namespacing,
e.g. \code{"palette.colours"}.}

\item{groups}{A \code{reactive()} yielding the character vector of group levels
currently in play.}

\item{default_palette}{A character vector of fallback colors.}

\item{defaults}{A named list of default values, or \code{NULL}. A named color
mapping stored under \code{key} seeds groups the user has not picked.}

\item{params}{Optional reactive-defaults store from
\code{\link[=setup_reactive_defaults]{setup_reactive_defaults()}}, or \code{NULL}. When \code{key} is backed by the store,
the mapping follows it rather than the client input, matching what
\code{\link[=setup_auto_update_logic]{setup_auto_update_logic()}} would have done for a direct \verb{input$<key>} read.}
}
\value{
A \code{\link[shiny:reactiveVal]{shiny::reactiveVal()}} holding a named character vector of colors
aligned to the current groups, or \code{NULL} before any groups exist. Call it
with no arguments to read, and with a value to seed.
}
\description{
A \code{\link[=multiColorPicker]{multiColorPicker()}} is rebuilt by \code{renderUI()} whenever the group set
changes, and the freshly built widget reports its value back on a client
round-trip. A plot that depends on the picker's raw \verb{input$<key>} therefore
rebuilds when that value lands, even at startup where the reported value is
exactly what the server had already seeded the picker with.
}
\details{
\code{setup_group_colors()} gives the module a server-side channel for the mapping
instead. It resolves the palette itself (via \code{\link[=resolve_palette]{resolve_palette()}}) as soon as
the group set is known, and holds the result in a \code{\link[shiny:reactiveVal]{shiny::reactiveVal()}},
which only invalidates on a \emph{changed} value. A rebuilt picker echoing the
mapping already in use therefore costs nothing, while a color the user
actually picks comes straight through.

Use it in three places:
\itemize{
\item Create the store next to the module's group-levels reactive.
\item Seed it inside the picker's \code{renderUI()} with the same
\code{initial_colors} the widget is built from, so the mapping is right even
when the render was deferred (a picker on a hidden tab is suspended).
\item Read \code{isolate_fn(store())} in the plot reactive, in place of
\verb{isolate_fn(input$<key>)}.
}

Note that \code{\link[shiny:freezeReactiveValue]{freezeReactiveValue()}} does not cover this case: inside a
\code{renderUI()} it pauses only the readers that run after it in that flush, and
at startup the plot output runs first.
}
\examples{
if (interactive()) {
    library(shiny)

    server <- function(input, output, session) {
        groups <- reactive(levels(as.factor(iris$Species)))
        palette_store <- setup_group_colors(
            input, "palette.colours", groups,
            default_palette = dittoViz::dittoColors()
        )

        output$palette.selection <- renderUI({
            initial_colors <- isolate(palette_store())
            multiColorPicker(
                session$ns("palette.colours"),
                groups = groups(), colors = initial_colors
            )
        })

        output$plot <- renderPlot(barplot(1:3, col = palette_store()))
    }
}
}
\seealso{
\code{\link[=resolve_palette]{resolve_palette()}}, \code{\link[=multiColorPicker]{multiColorPicker()}}, \code{\link[=setup_auto_update_logic]{setup_auto_update_logic()}}
}
\author{
Jared Andrews
}
