% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app_state.R
\name{sanitize_input_snapshot}
\alias{sanitize_input_snapshot}
\title{Strip transient keys from a Shiny input snapshot}
\usage{
sanitize_input_snapshot(inputs)
}
\arguments{
\item{inputs}{A named list of input values, typically produced by
\code{shiny::reactiveValuesToList(input)}.}
}
\value{
A named list containing only the serialisable, replayable inputs.
Returns an empty named list when \code{inputs} is \code{NULL} or empty.
}
\description{
Takes the result of \code{reactiveValuesToList(input)} and removes entries that
cannot (or should not) be persisted and replayed: DataTables row/search
state, plotly event streams, Shiny client-data, file-input payloads, and
action-button counters. The remaining named list contains only values that
can be handed back to a module's \code{*InputsUI(defaults = ...)} argument to
restore its configuration.
}
\examples{
sanitize_input_snapshot(list(
    "x.data" = "mpg",
    "table_rows_all" = 1:10,
    ".clientdata_output_height" = 400
))
}
\seealso{
\code{\link[=serialize_app_state]{serialize_app_state()}}
}
\author{
Jared Andrews
}
