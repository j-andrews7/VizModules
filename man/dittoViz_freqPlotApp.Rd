% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dittoViz_freqPlot_module_app.R
\name{dittoViz_freqPlotApp}
\alias{dittoViz_freqPlotApp}
\title{Create an example Modular freqPlot Shiny Application}
\usage{
dittoViz_freqPlotApp(
  data_list = NULL,
  defaults = NULL,
  hide.inputs = NULL,
  hide.tabs = NULL
)
}
\arguments{
\item{data_list}{An optional named list of data frames. If \code{NULL} (the default),
\code{list("composition" = example_composition)} is used as example data.}

\item{defaults}{A named list of input IDs and their default values to apply on startup.
An entry may also be a \code{\link[shiny:reactive]{shiny::reactive()}} or \code{\link[shiny:reactiveVal]{shiny::reactiveVal()}} to have the input
follow the parent app's state; see \code{\link[=setup_reactive_defaults]{setup_reactive_defaults()}}.}

\item{hide.inputs}{A character vector of input IDs to hide. Their values are still
initialized and used, but the controls are not shown in the UI.}

\item{hide.tabs}{A character vector of tab names to hide. Inputs in these tabs are
still initialized and used, but the controls are not shown in the UI.}
}
\value{
A Shiny app object.
}
\description{
This function generates a Shiny application with modular \code{\link[dittoViz:freqPlot]{dittoViz::freqPlot()}} components.
The app features a \strong{Data Import} section for uploading data,
a \strong{Data Table} for filtering the active dataset, and a \strong{Plot} area
for configuring and displaying an interactive frequency plot.
}
\details{
When \code{data_list} is not provided (or \code{NULL}), the app launches with
\code{example_composition} as an example dataset, which has twelve donors nested
inside two conditions - the shape \code{\link[dittoViz:freqPlot]{dittoViz::freqPlot()}} needs to compare
per-sample frequencies across groups.

This is a convenience wrapper around \code{\link[=createModuleApp]{createModuleApp()}}.
}
\examples{
library(VizModules)
# Launch with default example data:
app <- dittoViz_freqPlotApp()
if (interactive()) runApp(app)

# Launch on the cell-type composition of each donor, split by disease state:
app2 <- dittoViz_freqPlotApp(
    defaults = list(
        var = "cell_type", sample.by = "sample", group.by = "condition"
    )
)
if (interactive()) runApp(app2)
}
\seealso{
\code{\link[dittoViz:freqPlot]{dittoViz::freqPlot()}}, \code{\link[=dittoViz_freqPlotInputsUI]{dittoViz_freqPlotInputsUI()}},
\code{\link[=dittoViz_freqPlotOutputUI]{dittoViz_freqPlotOutputUI()}}, \code{\link[=dittoViz_freqPlotServer]{dittoViz_freqPlotServer()}}
}
\author{
Jared Andrews
}
