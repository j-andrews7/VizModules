% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dittoViz_scatterPlot_module_ui.R
\name{dittoViz_scatterPlotInputsUI}
\alias{dittoViz_scatterPlotInputsUI}
\title{Input UI components for the scatterPlot module}
\usage{
dittoViz_scatterPlotInputsUI(
  id,
  data,
  defaults = NULL,
  title = NULL,
  columns = 2
)
}
\arguments{
\item{id}{The ID for the Shiny module.}

\item{data}{The data frame used for plot generation.}

\item{defaults}{A named list of default values for the inputs.}

\item{title}{An optional title for the UI grid.}

\item{columns}{Number of columns for the UI grid.}
}
\value{
A Shiny tagList containing the UI elements
}
\description{
This should be placed in the UI where the inputs should be shown, with an \code{id}
that matches the \code{id} used in the \code{dittoViz_scatterPlotServer()} and \code{dittoViz_scatterPlotOutputUI()} functions.
}
\details{
The user inputs for this module are separated from the outputs to allow for
more flexible UI design.

The inputs will automatically be organized into a grid layout via the \code{organize_inputs()} function,
with \code{columns} controlling the number of columns in the grid.

Defaults can be set for each input by providing a named list of values to the \code{defaults} argument.
Nearly all parameters for \code{\link[dittoViz:scatterPlot]{dittoViz::scatterPlot()}} can be set via these inputs, so see the help
for that function for an exhaustive list.

Note that some of the parameters may have input types that differ from the actual function, e.g. \code{shape.panel}
is a text input for comma-separated integers, while the function expects a vector of integers.
The module will parse such inputs into the appropriate format for \code{\link[dittoViz:scatterPlot]{dittoViz::scatterPlot()}} automatically.

There are also a handful that are specific to the Shiny module that additionally modify the plotly output:
\itemize{
\item \code{id}: The ID for the Shiny module.
}
}
\section{Plot parameters not implemented or with altered functionality}{

The following \code{\link[dittoViz:scatterPlot]{dittoViz::scatterPlot()}} parameters are superseded by the enhanced Lines tab:
\itemize{
\item \code{add.xline} - Use \code{vline.intercepts} instead for vertical lines with full styling options
\item \code{add.yline} - Use \code{hline.intercepts} instead for horizontal lines with full styling options
\item \code{xline.linetype} - Use \code{vline.linetypes} instead
\item \code{xline.color} - Use \code{vline.colors} instead
\item \code{yline.linetype} - Use \code{hline.linetypes} instead
\item \code{yline.color} - Use \code{hline.colors} instead
}
The new Lines tab provides enhanced functionality including multiple lines per type,
individual line widths, opacities, and diagonal/ablines with slope control.
}

\examples{
library(VizModules)
data(mtcars)
dittoViz_scatterPlotInputsUI("scatterPlot", mtcars)
}
\seealso{
\code{\link[dittoViz:scatterPlot]{dittoViz::scatterPlot()}}, \code{\link[=organize_inputs]{organize_inputs()}},
\code{\link[=dittoViz_scatterPlotOutputUI]{dittoViz_scatterPlotOutputUI()}}, \code{\link[=dittoViz_scatterPlotServer]{dittoViz_scatterPlotServer()}}, \code{\link[=dittoViz_scatterPlotApp]{dittoViz_scatterPlotApp()}}
}
\author{
Jared Andrews
}
