% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotthis_DotPlot_module_ui.R
\name{plotthis_DotPlotInputsUI}
\alias{plotthis_DotPlotInputsUI}
\title{Input UI components for the DotPlot module}
\usage{
plotthis_DotPlotInputsUI(id, data, defaults = NULL, title = NULL, columns = 2)
}
\arguments{
\item{id}{The ID for the Shiny module.}

\item{data}{The data frame used for plot generation.}

\item{defaults}{A named list of default values for the inputs.}

\item{title}{An optional title for the UI grid.}

\item{columns}{Number of columns for the UI grid.}
}
\value{
A Shiny tagList containing the UI elements
}
\description{
This should be placed in the UI where the inputs should be shown, with an \code{id}
that matches the \code{id} used in the \code{plotthis_DotPlotServer()} and \code{plotthis_DotPlotOutputUI()} functions.
}
\details{
The user inputs for this module are separated from the outputs to allow for
more flexible UI design.

The inputs will automatically be organized into a grid layout via the \code{organize_inputs()} function,
with \code{columns} controlling the number of columns in the grid.

Defaults can be set for each input by providing a named list of values to the \code{defaults} argument.
Nearly all parameters for \code{\link[plotthis:DotPlot]{plotthis::DotPlot()}} can be set via these inputs, so see the help
for that function for an exhaustive list.
}
\section{Plot parameters not implemented or with altered functionality}{

The following \code{\link[plotthis:DotPlot]{plotthis::DotPlot()}} parameters are not available via UI inputs:
\itemize{
\item \code{xlab} - X-axis label (plotly allows interactive editing)
\item \code{ylab} - Y-axis label (plotly allows interactive editing)
\item \code{title} - Plot title (plotly allows interactive editing)
\item \code{subtitle} - Plot subtitle (not supported in plotly)
\item \code{aspect.ratio} - Aspect ratio control (handled by plotly layout)
\item \code{legend.position} - Legend positioning (plotly allows interactive repositioning)
\item \code{legend.direction} - Legend orientation (plotly allows interactive adjustment)
\item \code{x_sep} - Separator for multiple x columns (not yet implemented)
\item \code{y_sep} - Separator for multiple y columns (not yet implemented)
\item \code{split_by} - Split variable for separate plots (doesn't work with plotly; \code{facet_by} available instead)
\item \code{split_by_sep} - Separator for multiple \code{split_by} columns (\code{split_by} not used in module)
\item \code{size_name} - Size legend name (plotly allows interactive editing)
\item \code{fill_name} - Fill legend name (not yet implemented)
\item \code{fill_cutoff_name} - Fill cutoff legend name (not yet implemented)
\item \code{theme} - ggplot2 theme (managed internally)
\item \code{theme_args} - Theme arguments (not yet implemented)
\item \code{palcolor} - Managed internally via the palette selection UI
\item \code{x_text_angle} - X-axis text angle (handled by axis.tickangle.x)
\item \code{keep_empty} - Keep empty factor levels (not yet implemented)
\item \code{keep_na} - Keep NA values (not yet implemented)
\item \code{combine} - Combine multiple plots (not applicable as \code{split_by} is not implemented)
\item \code{seed} - Random seed (not applicable)
\item \code{nrow} - Only applies if \code{split_by} is used with combine (\code{split_by} not used in module)
\item \code{ncol} - Only applies if \code{split_by} is used with combine (\code{split_by} not used in module)
\item \code{byrow} - Only applies if \code{split_by} is used with combine (\code{split_by} not used in module)
\item \code{axes} - Only applies if \code{split_by} is used with combine (\code{split_by} not used in module)
\item \code{axis_titles} - Only applies if \code{split_by} is used with combine (\code{split_by} not used in module)
\item \code{guides} - Only applies if \code{split_by} is used with combine (\code{split_by} not used in module)
\item \code{design} - Only applies if \code{split_by} is used with combine (\code{split_by} not used in module)
}
}

\section{Plot parameters and defaults}{

The following \code{\link[plotthis:DotPlot]{plotthis::DotPlot()}} parameters can be accessed via UI inputs and/or the \code{defaults} argument:
\itemize{
\item \code{x} - X-axis variable (UI: "X Values", default: 2nd categorical variable)
\item \code{y} - Y-axis variable (UI: "Y Values", default: 3rd categorical variable)
\item \code{size_by} - Numeric column mapped to dot size (UI: "Size By", default: "" = count)
\item \code{size_min} - Minimum dot size (UI: "Min Dot Size", default: 1)
\item \code{size_max} - Maximum dot size (UI: "Max Dot Size", default: 10)
\item \code{fill_by} - Numeric column mapped to dot fill (UI: "Fill By", default: "")
\item \code{fill_cutoff} - Cutoff applied to the fill column (UI: "Fill Cutoff", default: NA)
\item \code{flip} - Flip the x and y axes (UI: "Rotate (swap X/Y)", default: FALSE)
\item \code{facet_by} - Faceting variable (UI: "Facet By", default: "")
\item \code{facet_scales} - Facet scale behavior (UI: "Facet Scale", default: "fixed")
\item \code{facet_ncol} - Number of facet columns (UI: "Columns", default: NULL)
\item \code{facet_nrow} - Number of facet rows (UI: "Rows", default: NULL)
\item \code{facet_byrow} - Facet ordering direction (UI: "Facet by Row", default: TRUE)
\item \code{palette} - Continuous fill palette (UI: "Color Palette", default: "Spectral")
\item \code{alpha} - Dot fill transparency (UI: "Alpha", default: 1)
}
}

\examples{
library(VizModules)
data(mtcars)
plotthis_DotPlotInputsUI("DotPlot", mtcars)
}
\seealso{
\code{\link[plotthis:DotPlot]{plotthis::DotPlot()}}, \code{\link[=organize_inputs]{organize_inputs()}},
\code{\link[=plotthis_DotPlotOutputUI]{plotthis_DotPlotOutputUI()}}, \code{\link[=plotthis_DotPlotServer]{plotthis_DotPlotServer()}}, \code{\link[=plotthis_DotPlotApp]{plotthis_DotPlotApp()}}
}
\author{
Jacob Martin, Jared Andrews
}
