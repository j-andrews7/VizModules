% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_reference_lines.R
\name{apply_plotly_newshape}
\alias{apply_plotly_newshape}
\title{Apply Plotly newshape styling from uniform Plotly inputs}
\usage{
apply_plotly_newshape(fig, input, isolate_fn = isolate)
}
\arguments{
\item{fig}{A plotly figure object.}

\item{input}{Shiny input object containing shape styling fields:
\code{shape.fill}, \code{shape.line.color}, \code{shape.line.width}, \code{shape.linetype},
\code{shape.opacity}.}

\item{isolate_fn}{Function to isolate reactive values. Defaults to
\code{shiny::isolate}.}
}
\value{
The modified plotly figure with updated \code{newshape} layout settings.
}
\description{
Applies user-drawn shape styling to a Plotly figure using inputs from
\code{\link[=uniform_plotly_inputs_ui]{uniform_plotly_inputs_ui()}}. Updates the \code{newshape} layout property to
style shapes drawn with Plotly's drawing tools (rectangles, circles, lines,
etc.) in the modebar.
}
\examples{
fig <- plotly::plot_ly(mtcars, x = ~wt, y = ~mpg, type = "scatter", mode = "markers")
shape_input <- list(
    shape.fill = "#ff000030", shape.line.color = "#ff0000",
    shape.line.width = 2, shape.linetype = "solid", shape.opacity = 0.5
)
apply_plotly_newshape(fig, shape_input, isolate_fn = identity)
}
\author{
Jared Andrews
}
