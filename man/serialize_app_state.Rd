% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app_state.R
\name{serialize_app_state}
\alias{serialize_app_state}
\title{Serialise a VizModules app state to JSON}
\usage{
serialize_app_state(state, pretty = TRUE)
}
\arguments{
\item{state}{A named list describing the app state. Any of the keys above may
be supplied; missing keys are simply omitted.}

\item{pretty}{Logical; pass-through to \code{\link[jsonlite:toJSON]{jsonlite::toJSON()}} controlling
whitespace/indentation. Defaults to \code{TRUE}.}
}
\value{
A length-one character string containing the JSON document.
}
\description{
Wraps a plain state \code{list} into the versioned VizModules app-state document
and encodes it as JSON with \code{\link[jsonlite:toJSON]{jsonlite::toJSON()}}. The \code{schema_version} field
(see \code{\link[=app_state_schema_version]{app_state_schema_version()}}) is always stamped onto the output, and a
capture timestamp is added under \code{app$timestamp} when one is not already
present.
}
\details{
The document is deliberately app-agnostic: any VizModules app can adopt the
same contract. The recognised top-level keys are:
\describe{
\item{\code{app}}{Metadata list (e.g. \code{name}, \code{vizmodules_version},
\code{timestamp}).}
\item{\code{app_inputs}}{Named list of app-level inputs (e.g. canvas
orientation, panel-label case).}
\item{\code{panels}}{An unnamed list of panel descriptors. Each panel is itself
a list with (at least) \code{module} (registry key), \code{dataset} (dataset name),
and \code{inputs} (a sanitised input snapshot); optional \code{label}, \code{filter},
and \code{geometry} entries round-trip when present.}
}
}
\examples{
state <- list(
    app = list(name = "figure-builder"),
    app_inputs = list(orientation = "portrait"),
    panels = list(
        list(module = "scatter", dataset = "example_iris",
             inputs = list("x.data" = "Sepal.Length"))
    )
)
cat(serialize_app_state(state))
}
\seealso{
\code{\link[=deserialize_app_state]{deserialize_app_state()}}, \code{\link[=sanitize_input_snapshot]{sanitize_input_snapshot()}}
}
\author{
Jared Andrews
}
