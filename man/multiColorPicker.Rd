% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multiColorPicker.R
\name{multiColorPicker}
\alias{multiColorPicker}
\title{Compact multi-group color picker input}
\usage{
multiColorPicker(
  inputId,
  label = NULL,
  groups,
  palette_options = NULL,
  selected_palette = NULL,
  colors = NULL,
  width = NULL,
  show_text = TRUE,
  compact = FALSE,
  panel = TRUE
)
}
\arguments{
\item{inputId}{Character. Shiny input id.}

\item{label}{Optional label displayed above the control.}

\item{groups}{Character or factor vector of group names.}

\item{palette_options}{Named list of palettes (each a character vector of
colors). Defaults to the palettes from \code{\link[=default_palettes]{default_palettes()}}.}

\item{selected_palette}{Optional name of the palette to preselect.}

\item{colors}{Optional named vector of starting colors. Values are matched to
\code{groups} by name when provided.}

\item{width}{Optional CSS width for the container.}

\item{show_text}{Logical. If \code{TRUE}, show editable hex text inputs beside the
color pickers.}

\item{compact}{Logical. If \code{TRUE}, renders a tighter layout with reduced
spacing, smaller controls, and narrower palette selector.}

\item{panel}{Logical. If \code{FALSE}, removes the surrounding panel/well styling
(border, padding, background).}
}
\value{
A UI element that produces a named character vector of colors.
}
\description{
Build a compact Shiny input that assigns colors to a set of groups using a
palette or manual hex pickers. The value returned to \code{input[[inputId]]} is a
named character vector of hex colors keyed by group.
}
\examples{
if (interactive()) {
    library(shiny)
    groups <- c("setosa", "virginica", "versicolor")

    ui <- fluidPage(
        multiColorPicker(
            "species_cols",
            "Species colors",
            groups = groups,
            selected_palette = "dittoColors"
        ),
        verbatimTextOutput("chosen")
    )

    server <- function(input, output, session) {
        output$chosen <- renderPrint(input$species_cols)
    }

    shinyApp(ui, server)
}
}
\author{
Jared Andrews
}
