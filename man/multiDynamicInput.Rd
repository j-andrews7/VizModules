% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multiDynamicInput.R
\name{multiDynamicInput}
\alias{multiDynamicInput}
\title{Dynamic multi-row input for repeating groups of inputs}
\usage{
multiDynamicInput(
  inputId,
  label = NULL,
  row_spec,
  elements = NULL,
  max_per_row = 4,
  add_label = "+ Add",
  width = NULL,
  panel = TRUE
)
}
\arguments{
\item{inputId}{Character. Shiny input id.}

\item{label}{Optional label displayed next to the add button.}

\item{row_spec}{A named list describing one row. Each element is itself a
list specifying a single field, with either:
\itemize{
\item \code{type} — a string alias: one of \code{"select"}, \code{"text"},
\code{"numeric"}, \code{"slider"}, \code{"checkbox"}, \code{"colour"}/\code{"color"}; or
\item \code{fn} — an input constructor function (e.g. \code{shiny::dateInput}).
}
plus an optional \code{args} list of arguments passed to the constructor
(everything except \code{inputId}, which is generated per row). The element name
becomes the key under which that field's value is returned.}

\item{elements}{Optional initial rows to display on startup. A named list of
rows (each a named list keyed by the \code{row_spec} names) in the same shape
as the return value. For example:
\code{list(models1 = list(model_type = "lm", formula = "y ~ x", line_colour = "#FF0000", line_width = 2))}.}

\item{max_per_row}{Integer. Maximum number of fields on one visual line
before wrapping. Default \code{4}.}

\item{add_label}{Character. Label for the add button. Default \code{"+ Add"}.}

\item{width}{Optional CSS width for the container.}

\item{panel}{Logical. If \code{FALSE}, removes the surrounding panel/well styling.}
}
\value{
A UI element that produces a named list of rows.
}
\description{
A custom Shiny input that lets users dynamically add and remove \strong{rows},
where each row is a group of heterogeneous inputs (e.g. a select, a text box,
a colour picker, and a numeric input on one line). A \code{+ Add} button appends a
row; an \code{X} button on each row deletes it. Rows are added and removed entirely
on the client by cloning a hidden template and letting Shiny bind the new
inputs, mirroring the architecture of \code{\link[=multiColorPicker]{multiColorPicker()}}.
}
\details{
The value reported to \code{input[[inputId]]} is a \strong{named list of rows}
(\code{model1}, \code{model2}, ...), each a named list keyed by the \code{row_spec} names.

The component is generic over input type. Each field in \code{row_spec} is
described by an input constructor and its arguments, so any Shiny input that
takes \code{inputId} as its first argument can be used. Common types have a short
\code{type = "..."} alias.
}
\examples{
if (interactive()) {
    library(shiny)

    ui <- fluidPage(
        multiDynamicInput(
            "models",
            label = "Models",
            row_spec = list(
                model_type  = list(type = "select",
                    args = list(choices = c("lm", "glm", "loess", "nls"))),
                formula     = list(type = "text",
                    args = list(placeholder = "y ~ poly(x, 2)")),
                line_colour = list(type = "colour", args = list(value = "#000000"))
            )
        ),
        verbatimTextOutput("chosen")
    )

    server <- function(input, output, session) {
        output$chosen <- renderPrint(input$models)
    }

    shinyApp(ui, server)
}
}
\author{
Jacob Martin
}
