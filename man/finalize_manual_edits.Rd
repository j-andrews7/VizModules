% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_helpers.R
\name{finalize_manual_edits}
\alias{finalize_manual_edits}
\title{Render persistent manual plot layout edits across re-renders}
\usage{
finalize_manual_edits(
  fig,
  plot_source,
  store,
  session,
  regen_keys = character(0)
)
}
\arguments{
\item{fig}{A plotly figure object, typically the result of your
plotting pipeline. If \code{NULL}, it is returned
unchanged.}

\item{plot_source}{Character scalar. The \strong{same} plotly event source id
passed to \code{\link[=setup_manual_edits]{setup_manual_edits()}}; assigned to \code{fig$x$source}.}

\item{store}{The list returned by \code{\link[=setup_manual_edits]{setup_manual_edits()}}.}

\item{session}{The module's \code{session} object, used to namespace the
colorbar drag input.}

\item{regen_keys}{Character vector of annotation keys (e.g. \code{"axis:y"}) whose
\code{text} is regenerated on every rebuild and must not be overwritten by a
captured edit. Pass the axis side(s) carrying an active data adjustment so
the freshly built label (e.g. \code{"log2(units)"}) always wins, while the
captured position still persists. Defaults to none (all captured props are
restored, the pre-existing behaviour).}
}
\value{
The finalized plotly figure, ready to be returned from
\code{\link[plotly:renderPlotly]{plotly::renderPlotly()}}.
}
\description{
Render-step companion to \code{\link[=setup_manual_edits]{setup_manual_edits()}}. Call this inside
your module's \code{\link[plotly:renderPlotly]{plotly::renderPlotly()}} on the freshly rebuilt
figure, immediately before returning it.
}
\details{
It performs four jobs:
\enumerate{
\item tags the figure with the module's plotly event \code{source} so its
\code{plotly_relayout} events are captured by \code{setup_manual_edits()};
\item restores any manually repositioned legend, annotations, axis titles,
and colorbar captured so far;
\item records the figure so future relayout events can be matched to stable
annotation keys (surviving re-ordering on rebuild); and
\item attaches the JavaScript listener that forwards colorbar drags.
}

Restored edits are applied under \code{\link[shiny:isolate]{shiny::isolate()}} so re-applying
them never triggers an additional re-render.
}
\examples{
\dontrun{
# Inside renderPlotly(), after building `fig`:
fig <- finalize_manual_edits(fig, plot_source, edit_store, session)
fig
}
}
\seealso{
\code{\link[=setup_manual_edits]{setup_manual_edits()}} for the setup-step companion.
}
\author{
Jared Andrews
}
