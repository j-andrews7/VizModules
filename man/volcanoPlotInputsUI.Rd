% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/volcanoPlot_module_ui.R
\name{volcanoPlotInputsUI}
\alias{volcanoPlotInputsUI}
\title{Input UI components for the volcanoPlot module}
\usage{
volcanoPlotInputsUI(
  id,
  data,
  defaults = NULL,
  title = "Volcano Settings",
  columns = 2
)
}
\arguments{
\item{id}{The ID for the Shiny module.}

\item{data}{The data frame used for plot generation.}

\item{defaults}{A named list of default values for the inputs.}

\item{title}{An optional title for the UI grid.}

\item{columns}{Number of columns for the UI grid.}
}
\value{
A Shiny tagList containing the UI elements
}
\description{
This should be placed in the UI where the inputs should be shown, with an \code{id}
that matches the \code{id} used in the \code{volcanoPlotServer()} and \code{volcanoPlotOutputUI()} functions.
}
\details{
The user inputs for this module are separated from the outputs to allow for
more flexible UI design.

The inputs will automatically be organized into a grid layout via the \code{\link[=organize_inputs]{organize_inputs()}} function,
with \code{columns} controlling the number of columns in the grid.

Defaults can be set for each input by providing a named list of values to the \code{defaults} argument.
Nearly all parameters for \code{\link[dittoViz:scatterPlot]{dittoViz::scatterPlot()}} can be set via these inputs, so see the help
for that function for an exhaustive list.

Additional inputs specific to volcano plots are added to control significance thresholds and colors:
\itemize{
\item \code{sig.thresh}: Significance threshold (default 0.05)
\item \code{fc.thresh}: Log2 fold change threshold (default 0)
\item \code{color.up}: Color for upregulated genes (default "red")
\item \code{color.down}: Color for downregulated genes (default "blue")
\item \code{color.ns}: Color for non-significant genes (default "lightgray")
}
}
\examples{
library(vizModules)
data(airway_deseq2)
volcanoPlotInputsUI("volcanoPlot", airway_deseq2)
}
\seealso{
\code{\link[dittoViz:scatterPlot]{dittoViz::scatterPlot()}}, \code{\link[=organize_inputs]{organize_inputs()}},
\code{\link[=volcanoPlotOutputUI]{volcanoPlotOutputUI()}}, \code{\link[=volcanoPlotServer]{volcanoPlotServer()}},
\code{\link[=volcanoPlotApp]{volcanoPlotApp()}}
}
\author{
Jared Andrews
}
