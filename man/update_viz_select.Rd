% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/select_inputs.R
\name{update_viz_select}
\alias{update_viz_select}
\title{Update a select input created by viz_select_input}
\usage{
update_viz_select(session, inputId, choices = NULL, selected = NULL, ...)
}
\arguments{
\item{session}{The \code{session} object passed to the module server function.}

\item{inputId}{The id of the input to update.}

\item{choices}{New choices for the input, or \code{NULL} to leave them unchanged.}

\item{selected}{New value(s) to select, or \code{NULL} to leave the selection
unchanged.}

\item{...}{Further arguments passed to
\code{\link[shinyWidgets:updateVirtualSelect]{shinyWidgets::updateVirtualSelect()}}.}
}
\value{
No return value, called for its side effect of updating the input.
}
\description{
Companion to \code{\link[=viz_select_input]{viz_select_input()}}, wrapping
\code{\link[shinyWidgets:updateVirtualSelect]{shinyWidgets::updateVirtualSelect()}} so the empty-string "no selection"
choice is relabelled consistently with the UI side.
}
\examples{
library(shiny)
library(VizModules)

server <- function(input, output, session) {
    observeEvent(input$reset, {
        update_viz_select(session, "gene", choices = c("", "GENE1", "GENE2"))
    })
}
}
\seealso{
\code{\link[=viz_select_input]{viz_select_input()}}, \code{\link[shinyWidgets:updateVirtualSelect]{shinyWidgets::updateVirtualSelect()}}
}
\author{
Jared Andrews
}
